* Re-initialise git repository
* Remove unused directories in dotfiles (emacs, stumpwm etc.)
* Send all old files to appropriate archive directories
** Keep list of music instead of the actual files; clear out unnecessary films
* Copy full backup to another external hard drive
* Format and encrypt hard drive
* Add alias for checking todos to bash:
[source,sh]
-------------------------------
head -2 $(find -H . -name TODO)
-------------------------------
* init git repositories for all other projects
